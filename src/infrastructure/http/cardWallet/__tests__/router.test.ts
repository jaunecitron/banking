describe('Wallet Router', () => {
  it.todo('Should route on loadCardFromWallet on POST /wallet/:walletId/load/card/:cardId');
  it.todo('Should route on unloadCard on POST /card/:cardId/unload');
  it.todo('Should route on unloadCard on POST /card/:cardId/block');
});
